import styled from "styled-components";

const LayoutWrapper = styled("div")(() => ({
  backgroundColor: "rgb(181, 210, 214)",
  width: "100%",
  height: "100vh",
  padding: "4rem",
  display: "flex",
}));

export default LayoutWrapper;
