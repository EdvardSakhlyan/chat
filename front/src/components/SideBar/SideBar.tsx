import { FC } from "react"

interface ISideBarProps {
    routs: []
}

const SideBar: FC<ISideBarProps> = () => {
    return (
        <nav>
            <ul>
                {

                }
            </ul>
        </nav>
    )
}

export default SideBar